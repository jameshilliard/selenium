load("@rules_ruby//ruby:defs.bzl", "rb_test")

[
    rb_test(
        name = file[:-8],
        srcs = [file],
        args = ["rb/spec/unit/selenium/webdriver/devtools/" + file],
        main = "@bundle//:bin/rspec",
        deps = ["@bundle"],
    )
    for file in glob(["*_spec.rb"])
]
